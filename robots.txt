User-agent: *
Disallow: /
Allow: /$ ```
This tells polite scrapers (Google, Bing) not to index subpages. Malicious scrapers ignore it.

**2. Don't put sensitive links in the HTML source** — This is the only real defense. Your current code has all category links as plain HTML in the page source even before the door "opens." If you want to hide them from scrapers, the links need to come from an external source fetched only after the door opens:

```javascript
// Instead of hardcoded HTML links, fetch from a JSON file:
fetch('links.json').then(function(r) { return r.json(); }).then(function(links) {
    // Build the category list dynamically
});
